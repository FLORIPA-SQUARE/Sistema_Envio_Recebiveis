# === Portas do Sistema ===
# Altere aqui se houver conflito com outros sistemas na maquina
FRONTEND_PORT=21555
BACKEND_PORT=21556
POSTGRES_PORT=21434

# === PostgreSQL (Docker) ===
POSTGRES_USER=boletos_user
POSTGRES_PASSWORD=boletos_dev_2026
POSTGRES_DB=boletos_db

# === Backend (FastAPI) ===
DATABASE_URL=postgresql+asyncpg://boletos_user:boletos_dev_2026@localhost:21434/boletos_db
JWT_SECRET_KEY=CHANGE_ME_TO_A_RANDOM_SECRET_KEY
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=8

# Diretorio de armazenamento de arquivos (PDFs, XMLs, renomeados)
# Padrao: backend/storage (relativo ao projeto)
# STORAGE_DIR=C:\caminho\customizado\storage

# === Conta SMTP (envio de emails) ===
# Servidor e porta
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Credenciais de autenticacao
SMTP_USER=cobranca@empresa.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Seguranca
SMTP_USE_TLS=true

# Remetente (aparece no "De:" do email)
SMTP_FROM_EMAIL=cobranca@empresa.com
SMTP_FROM_NAME=Equipe de Cobranca
